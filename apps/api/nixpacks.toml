[phases.setup]
nixPkgs = ["nodejs_22"]

[phases.install]
cmds = ["cd ../.. && npm install"]

[phases.build]
cmds = ["cd ../.. && npx tsc --project apps/api/tsconfig.json"]

[start]
cmd = "cd ../.. && node apps/api/dist/index.js"
